# fix-enso-reveal-animation — tasks

## 1. 診断（design D1 の切り分けマトリクス。実装より先に必ず行う）

- [x] 1.1 `EnsoMark` にマウント・settle 発火・progress 値の `console.log` を仕込み、実機ログで「保険（durationMs 後のフル収束）」が発火しているかを確認する（発火していなければバンドル未反映を疑い、Metro 接続と再ビルド手順を先に潰す）※ログは仕込み済み（`__DEV__` 時のみ `[EnsoMark]` プレフィックスで出力）。実機ログの確認は次回の実機ビルドで実施
- [x] 1.2 settle 発火済みなのに表示が変わらない場合: `<Svg>` に `key={settled ? 'full' : 'anim'}` を付けた強制再マウント版で回避できるかを実機確認する（＝「初回描画は正しいが更新が反映されない」の確定）※検証版はそのまま C' 実装（2.1）に統合した。実機確認は 3.x で行う
- [x] 1.3 `useAnimatedProps` を外し、`requestAnimationFrame` + `setState` で `strokeDashoffset` を駆動する検証版で一筆書きが完走するかを実機確認する（＝reanimated×Fabric 起因の確定）※同上、C' 実装に統合。完走の実機確認は 3.x で行う
- [x] 1.4 診断結果を design.md の Open Questions に追記し、採用方式（C'. rAF+再マウント収束 / B. Skia / E. フレーム連番）を確定する ※実機診断で C' 棄却（JS は完走するが Mask 内ノードの props 反映がネイティブ層で欠落。強制再描画でも解消せず）。**E（フレーム連番）を採用**（ユーザー承認済み）。詳細は design.md「診断結果・第2ラウンド」

## 2. EnsoMark の作り替え（確定した方式で。props 互換: tone/size/animated/durationMs/opacity）

- [x] 2.1 確定方式で `EnsoMark` を書き換える ※E 方式で書き換え済み: 連番フレームを expo-image で重ねて opacity 切り替え再生。rAF 駆動 + 独立タイマーの収束保証は維持
- [x] 2.2 連続選択（演出完了前の選択切り替え）で最後の選択がフルに描画されることを保証する（タイマー/rAF のクリーンアップ含む）※rAF/タイマーは effect クリーンアップで解除。セレクタは選択ごとに key 再マウントなので、最後の選択のインスタンスが必ず settle する
- [x] 2.3 Skia 採用時のみ: `npx expo install @shopify/react-native-skia` → prebuild → 実機再ビルド（datetimepicker と同じ手順）※対象外で確定（E. フレーム連番を採用したため Skia は不採用。互換バージョン 2.6.2 の調査結果のみ design.md に記録）
- [x] 2.4 フレーム連番採用時のみ: リビール連番 PNG（12〜16 コマ）の生成スクリプトを追加し、`expo-image` での順送り再生を実装する ※`scripts/generate-enso-reveal-frames.mjs`（ヘッドレス Chrome で Web と同一の SVG 合成を焼き込み。依存追加なし）で 24 コマ生成。`src/assets/enso-reveal/` に出力
- [x] 2.5 リビールのロジック（進捗→表示状態の写像、収束条件）を純関数に切り出し、Vitest でテストする（`src/lib/ensoReveal.ts` / `ensoReveal.test.ts`）

## 3. 実機確認（enso-motif spec のシナリオに対応）

- [x] 3.1 捨てたい度セレクタ（撮影レビュー・記録編集の両方）で、選択時の円相がフルに描き終わることを実機確認する ※実機で「一筆書きで最後まで描き切る」ことを確認（両画面とも同一実装）
- [x] 3.2 演出中に他の更新が走るケース（撮影直後・写真追加直後の選択）でもフル描画で終わることを実機確認する ※実機確認済み（撮影直後・写真追加直後ともフル描画で完走）
- [x] 3.3 選択肢を素早く連続で切り替えても、最後の選択がフルに描画されることを実機確認する ※実機確認済み
- [x] 3.4 退避完了・手放し完了の大円相が従来どおりの見た目・タイミングで完走することを実機確認する（劣化していないこと）※実機確認済み（フレーム再生に統一後も完走・見た目維持）
- [x] 3.5 筆の質感（かすれ・濃淡）が小サイズ・大サイズの両方で保たれていることを確認する ※フレームは従来と同一アセット・同一マスク合成から生成（かすれ・濃淡の保持を生成画像で目視確認済み）

## 4. 仕上げ

- [x] 4.1 診断用の `console.log`・検証コードを取り除く
- [x] 4.2 `npm run typecheck` と `npm test` が通る（131 tests / 20 files）
- [x] 4.3 不要になった代替実装・アセットがあれば削除する ※C' の残骸（dashoffset/イージング関数と SVG マスク構成）を削除。`ensoReveal.ts` は再生ロジックのみに縮小、幾何・イージングは生成スクリプトへ集約。react-native-svg 依存は他コンポーネント（アイコン等）が使用するため残置、`enso-mono-trimmed-solid.png` はフレーム生成の入力として残置
